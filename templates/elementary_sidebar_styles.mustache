{{!
    Elementary Sidebar Styles for remui_kids theme
    
    This template provides the CSS styles for the elementary sidebar.
    Include this in any page that uses the elementary sidebar.
}}

<style>
    /* Sidebar Styles */
    .student-sidebar {
        position: fixed;
        left: 0;
        top: 0;
        width: 280px;
        height: 100vh;
        background: linear-gradient(180deg, #667eea 0%, #764ba2 100%);
        color: white;
        z-index: 1000;
        overflow-y: auto;
        box-shadow: 2px 0 10px rgba(0,0,0,0.1);
    }
    
    .sidebar-nav {
        padding: 20px 0;
    }
    
    .nav-section {
        margin-bottom: 30px;
    }
    
    .section-title {
        padding: 0 20px 10px 20px;
        font-size: 0.8rem;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 1px;
        color: rgba(255,255,255,0.7);
        border-bottom: 1px solid rgba(255,255,255,0.1);
        margin-bottom: 15px;
        display: flex;
        align-items: center;
        gap: 8px;
    }
    
    .section-bullet {
        width: 8px;
        height: 8px;
        border-radius: 50%;
    }
    
    .section-bullet.blue { background: #3498db; }
    .section-bullet.green { background: #2ecc71; }
    .section-bullet.orange { background: #f39c12; }
    .section-bullet.purple { background: #9b59b6; }
    
    .nav-list {
        list-style: none;
        padding: 0;
        margin: 0;
    }
    
    .nav-item {
        margin-bottom: 5px;
    }
    
    .nav-link {
        display: flex;
        align-items: center;
        padding: 12px 20px;
        color: rgba(255,255,255,0.8);
        text-decoration: none;
        transition: all 0.3s ease;
        border-left: 3px solid transparent;
    }
    
    .nav-link:hover {
        background: rgba(255,255,255,0.1);
        color: white;
        text-decoration: none;
        border-left-color: rgba(255,255,255,0.3);
    }
    
    .nav-link.active {
        background: rgba(255,255,255,0.15);
        color: white;
        border-left-color: #fff;
    }
    
    .nav-link i {
        margin-right: 12px;
        width: 20px;
        text-align: center;
        font-size: 1.1rem;
    }
    
    /* Quick Actions */
    .quick-actions-container {
        padding: 0 20px;
    }
    
    .quick-action-card {
        background: rgba(255,255,255,0.1);
        border-radius: 12px;
        padding: 15px;
        margin-bottom: 12px;
        cursor: pointer;
        transition: all 0.3s ease;
        border: 1px solid rgba(255,255,255,0.1);
    }
    
    .quick-action-card:hover {
        background: rgba(255,255,255,0.2);
        transform: translateY(-2px);
    }
    
    .quick-action-icon {
        display: flex;
        align-items: center;
        gap: 10px;
        margin-bottom: 8px;
    }
    
    .quick-action-icon i {
        font-size: 1.2rem;
    }
    
    .quick-action-content h4 {
        margin: 0 0 5px 0;
        font-size: 0.9rem;
        font-weight: 600;
        color: white;
    }
    
    .quick-action-content p {
        margin: 0;
        font-size: 0.75rem;
        color: rgba(255,255,255,0.8);
        line-height: 1.3;
    }
    
    .quick-action-arrow {
        display: flex;
        justify-content: flex-end;
        margin-top: 8px;
    }
    
    .quick-action-arrow i {
        font-size: 0.8rem;
        color: rgba(255,255,255,0.6);
    }
    
    /* Main Content Area with Sidebar */
    .main-content-with-sidebar {
        margin-left: 280px;
        min-height: 100vh;
        background: #f8f9fa;
    }
    
    /* Quick Action Card Active State */
    .quick-action-card.active {
        background: rgba(255,255,255,0.25) !important;
        border: 2px solid rgba(255,255,255,0.5) !important;
        transform: translateY(-2px) !important;
    }
    
    .quick-action-card.active .quick-action-icon {
        color: #fff !important;
    }
    
    .quick-action-card.active .quick-action-content h4 {
        color: #fff !important;
    }
    
    .quick-action-card.active .quick-action-content p {
        color: rgba(255,255,255,0.9) !important;
    }
    
    .quick-action-card.active .quick-action-arrow i {
        color: rgba(255,255,255,0.8) !important;
    }
</style>

